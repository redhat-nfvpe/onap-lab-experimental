#!/bin/bash
set -e

if [ "$EUID" == 0 ]; then
	echo "Do not run this script as root"
	exit 1
fi

HERE=$(dirname "$(readlink -f "$0")")
ROOT=$(readlink -f "$HERE/..")
. "$ROOT/configuration/environment"
. "$ROOT/utils/functions"

RESULTS="$WORKSPACE_ROOT/results"

m "Fetching results to $(q "$RESULTS")"

if [ "$1" == '-c' ]; then
	rm --recursive --force "$RESULTS" 
fi

mkdir --parents "$RESULTS/configuration/tripleo/"
mkdir --parents "$RESULTS/logs/hypervisor/"
mkdir --parents "$RESULTS/logs/tripleo/"

function r () {
	"$HERE/rsync" \
		--rsync-path="sudo rsync" \
		--recursive \
		--copy-links \
		--ignore-missing-args \
		--prune-empty-dirs \
		"$@"
}

r \
	--include=*/ \
	--include=*.sh \
	--include=*.conf \
	--include=*.yaml \
	--include=*.json \
	--exclude=* \
	tripleo-stack:/home/stack/ \
	tripleo-stack:/var/lib/mistral \
	"$RESULTS/configuration/tripleo/" || true

r \
	--include=*/ \
	--include=*.log \
	--exclude=* \
	hypervisor-stack:/home/stack/ \
	hypervisor-stack:/var/log/libvirt/qemu \
	hypervisor-stack:/var/log/virtualbmc \
	"$RESULTS/logs/hypervisor/" || true

r \
	--include=*/ \
	--include=*.log \
	--include=*.tar.gz \
	--exclude=* \
	tripleo-stack:/home/stack/ \
	tripleo-stack:/var/log/containers/ \
	"$RESULTS/logs/tripleo/" || true

find "$RESULTS/logs/tripleo/" -type f -name "*.tar.gz" |
while read F; do
	D=${F%.tar.gz}
	mkdir --parents "$D"
	tar xf "$F" -C "$D"
done
